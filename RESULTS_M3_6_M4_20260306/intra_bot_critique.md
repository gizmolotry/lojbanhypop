# Intra-Bot Critique Snapshot

- Franklin: high risk that swap generation can touch non-semantic header tokens; infer_swap_semantics is currently brittle and under-classifies symmetric cases.
- Nash: metric schema should keep explicit per-cell total/symmetric/asymmetric accuracies plus confusion matrix and secondary truth/canonical rates.

Interpretation of current M4 instrumentation output:
- OP inventory collapsed to a single operator id in this checkpoint, so paraphrase consistency is inflated and not evidence of healthy predicate families.
- This should be treated as a collapse signal and investigated before claiming predicate-family emergence.
